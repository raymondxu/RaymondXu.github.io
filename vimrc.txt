set backspace=indent,eol,start
set history=50
set ruler
set showcmd
set incsearch
set ignorecase
set hlsearch

nnoremap j gj
nnoremap k gk

if has('mouse')
  set mouse=a
endif

if &t_Co > 2 || has("gui_running")
  syntax on
  set hlsearch
endif

set number
set shiftwidth=4
set ts=4
set backupdir^=~/.backup
set dir^=~/.backup//

" Save and quit with just the right hand ;)
inoremap jk <ESC>
nnoremap l; :x
